# Windows-Idle-Control-Keeper
W.I.C.K detects Idle activity with your specific thresholds, and tiggers and/or prevents Windows Sleep, on Your terms. Detects activity on CPU, network, storage, mouse, and peak sound value, counting idle-breaking instances per time period. It does not affect and is not affected by (auto) windows screen locking, or (auto) turning off the display.
